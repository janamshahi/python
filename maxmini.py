number = {
    "a": 24,
    "b": 36,
    "c": 48,
    "d": 60,
}
max_value = max(number.values())
min_value = min(number.values())
print("Maximum value:", max_value)
print("Minimum value:", min_value)